{
  "name": "monorepo",
  "private": true,
  "workspaces": [
    "../backend-repo",
    "../fe-repo",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "packageManager": "npm@9.5.1",
  "engines": {
    "node": ">=18.0.0"
  }
}